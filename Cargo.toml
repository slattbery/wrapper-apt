[package]
name = "apt"
version = "0.1.0"
edition = "2024"

[dependencies]
getch = "0.3.1"
phf = { version = "0.14.0", features = ["macros"] }

[features]
confirmation = []

[profile.release]
opt-level = 3
debug = "none"
strip = true
lto = true
panic = "abort"
codegen-units = 1
