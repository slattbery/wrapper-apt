[package]
name = "apt"
version = "0.1.0"
edition = "2024"

[dependencies]
phf = { version = "0.14.0", features = ["macros"] }

[profile.release]
opt-level = 3
debug = "none"
strip = true
lto = true
panic = "abort"
codegen-units = 1
